<UserControl x:Class="StudentInfoManagement.Views.Student.StudentCoursesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1100"
             FontFamily="Segoe UI"
             Background="#F9FAFB">

    <UserControl.Resources>
        <SolidColorBrush x:Key="PrimaryBlue" Color="#2563EB"/>
        <SolidColorBrush x:Key="SuccessGreen" Color="#16A34A"/>
        <SolidColorBrush x:Key="TextDark" Color="#111827"/>
        <SolidColorBrush x:Key="BorderColor" Color="#D1D5DB"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.03" BlurRadius="5" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FilterLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextDark}"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="CleanComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="5,0,5,15">
            <TextBlock Text="Đăng ký môn học" FontSize="26" FontWeight="Bold" Foreground="{StaticResource TextDark}"/>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="20" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Tìm kiếm &amp; Lọc" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Tìm kiếm môn học" Style="{StaticResource FilterLabelStyle}"/>
                        <Border Height="40" CornerRadius="6" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Background="White">
                            <Grid>
                                <TextBlock Text="🔍" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="#9CA3AF"/>
                                <TextBox x:Name="txtSearch" BorderThickness="0" Background="Transparent"
                                         VerticalContentAlignment="Center" Margin="35,0,5,0" TextChanged="txtSearch_TextChanged"/>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Lọc theo Khoa" Style="{StaticResource FilterLabelStyle}"/>
                        <ComboBox x:Name="cboDepartmentFilter" Style="{StaticResource CleanComboBoxStyle}" SelectionChanged="cboDepartmentFilter_SelectionChanged" >
            
                            <ComboBoxItem Content="Tất cả các khoa" IsSelected="True"/>
                            <ComboBoxItem Content="Khoa CNTT"/>
                            <ComboBoxItem Content="Khoa Kinh Tế"/>
                            <ComboBoxItem Content="Khoa Ngoại ngữ"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.8*"/>
                        <ColumnDefinition Width="1.4*"/>
                        <ColumnDefinition Width="0.3*"/>
                        <ColumnDefinition Width="0.8*"/>
                        <ColumnDefinition Width="0.8*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Mã HP" FontWeight="Bold"/>
                    <TextBlock Grid.Column="1" Text="Tên môn học" FontWeight="Bold"/>
                    <TextBlock Grid.Column="2" Text="TC" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Grid.Column="3" Text="Sĩ số" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Grid.Column="4" Text="Khoa" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Grid.Column="5" Text="Thao tác" FontWeight="Bold" HorizontalAlignment="Center"/>
                </Grid>

                <Separator Grid.Row="1" Margin="0,0,0,10"/>

                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="ListHocPhan">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="0.8*"/>
                                        <ColumnDefinition Width="1.4*"/>
                                        <ColumnDefinition Width="0.3*"/>
                                        <ColumnDefinition Width="0.8*"/>
                                        <ColumnDefinition Width="0.8*"/>
                                        <ColumnDefinition Width="1*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="{Binding MaHocPhan}" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="{Binding TenMonHoc}" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" Text="{Binding TinChi}" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                                    <Border Grid.Column="3" Background="#F3F4F6" CornerRadius="4" Padding="8,2" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding SiSoHienThi}" FontWeight="SemiBold" Foreground="#2563EB"/>
                                    </Border>

                                    <TextBlock Grid.Column="4" Text="{Binding Khoa}" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                                    <Button Grid.Column="5"
                                            Content="Đăng ký"
                                            
                                            Tag="{Binding MaHocPhan}"  Click="BtnRegister_Click"
                                            Background="{Binding ButtonColor}"
                                            IsEnabled="{Binding CoTheDangKy}"
                                            Foreground="White"
                                            Padding="8,5" Width="90"
                                            BorderThickness="0"
                                            FontWeight="SemiBold"
                                            Cursor="Hand"
                                            HorizontalAlignment="Center">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="4"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>