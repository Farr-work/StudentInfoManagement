<UserControl x:Class="StudentInfoManagement.Views.StudentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" Background="White" Width="1128">

    <UserControl.Resources>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#2563EB"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="Auto" MinHeight="67"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Text="Quản Lý Sinh Viên"
                   FontSize="24"
                   VerticalAlignment="Top" Margin="20,28,0,0" Height="42" FontWeight="Bold"/>

        <Grid Grid.Row="1" Margin="20,10,20,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchTextBox" 
             Grid.Column="0"
             VerticalContentAlignment="Center"
             BorderBrush="Black" 
             BorderThickness="1" 
             Margin="0,8,10,4"
             TextChanged="SearchTextBox_TextChanged"
             ToolTip="Nhập Mã SV hoặc Tên để tìm kiếm..."/> 

            <Button Grid.Column="1" Width="100" Margin="0,10,10,4" Foreground="#2563EB" Click="Button_Click_1">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border BorderThickness="1" BorderBrush="#2563EB" CornerRadius="5">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" >
                                <TextBlock Text="📥" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Xuất CSV" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button Grid.Column="2" Content="Add New Students" Width="130" Margin="0,10,0,4" Foreground="White" Click="Button_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border BorderThickness="1" Background="#2563EB" BorderBrush="#2563EB" CornerRadius="5">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" >
                                <TextBlock Text="➕" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Thêm Sinh Viên" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>

        <Grid Grid.Row="2" Margin="20,20,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Label Content="Danh Sách Sinh Viên" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

            <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="2" CornerRadius="10" Margin="0">
                <DataGrid x:Name="StudentsGrid"
                          Grid.Row="1"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          HeadersVisibility="Column"
                          Background="White"
                          BorderThickness="0"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#E5E7EB"
                          RowHeight="48"
                          ColumnHeaderHeight="40" Margin="8,8,8,8">

                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="Padding" Value="10"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#E5E7EB"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="10,0"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã Sinh Viên" Binding="{Binding masv}" Width="100"/>
                        <DataGridTextColumn Header="Họ tên" Binding="{Binding hoten}" Width="200"/>
                        <DataGridTextColumn Header="Tên Lớp" Binding="{Binding tenlop}" Width="100"/>
                        <DataGridTextColumn Header="Giới Tính" Binding="{Binding gioitinh}" Width="80"/>
                        <DataGridTextColumn Header="Địa chỉ" Binding="{Binding diachi}" Width="150"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding email}" Width="150"/>
                        <DataGridTextColumn Header="Số Điện Thoại" Binding="{Binding sdt}" Width="100"/>
                        <DataGridTextColumn Header="Trạng Thái" Binding="{Binding trangthai}" Width="100"/>

                        <DataGridTemplateColumn Header="Thao Tác" Width="100" IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <Button Click="EditStudent_Click" Margin="0,0,10,0" ToolTip="Sửa" Background="Transparent" BorderThickness="0" Cursor="Hand">
                                            <TextBlock Text="✏️" FontSize="16"/>
                                        </Button>
                                        <Button Click="DeleteStudent_Click" Margin="0,0" ToolTip="Xóa" Background="Transparent" BorderThickness="0" Cursor="Hand">
                                            <TextBlock Text="🗑️" FontSize="16" Foreground="Red"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <Border Background="White" x:Name="box" Visibility="Hidden" BorderBrush="Black" BorderThickness="1" Margin="0"  Width="490" Grid.RowSpan="3" Height="500" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid Width="488" Height="490">
                <Label Content="Thông Tin Sinh Viên" HorizontalAlignment="Center" Margin="0,10,0,0" VerticalAlignment="Top" FontWeight="Bold" FontSize="18"/>

                <Grid Margin="20,50,20,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="60"/>
                    </Grid.RowDefinitions>

                    <Label Content="Mã Sinh Viên:" Grid.Row="0" VerticalAlignment="Center"/>
                    <TextBox x:Name="MasvTextBox" Grid.Row="0" Grid.Column="1" VerticalContentAlignment="Center" BorderBrush="Black" BorderThickness="1" Height="30"/>

                    <Label Content="Họ Tên:" Grid.Row="1" VerticalAlignment="Center"/>
                    <TextBox x:Name="HotenTextBox" Grid.Row="1" Grid.Column="1" VerticalContentAlignment="Center" BorderBrush="Black" BorderThickness="1" Height="30"/>

                    <Label Content="Tên Lớp:" Grid.Row="2" VerticalAlignment="Center"/>
                    <TextBox x:Name="TenlopTextBox" Grid.Row="2" Grid.Column="1" VerticalContentAlignment="Center" BorderBrush="Black" BorderThickness="1" Height="30"/>

                    <Label Content="Giới Tính:" Grid.Row="3" VerticalAlignment="Center"/>
                    <TextBox x:Name="GioitinhTextBox" Grid.Row="3" Grid.Column="1" VerticalContentAlignment="Center" BorderBrush="Black" BorderThickness="1" Height="30"/>

                    <Label Content="Địa Chỉ:" Grid.Row="4" VerticalAlignment="Center"/>
                    <TextBox x:Name="DiachiTextBox" Grid.Row="4" Grid.Column="1" VerticalContentAlignment="Center" BorderBrush="Black" BorderThickness="1" Height="30"/>

                    <Label Content="Email:" Grid.Row="5" VerticalAlignment="Center"/>
                    <TextBox x:Name="EmailTextBox" Grid.Row="5" Grid.Column="1" VerticalContentAlignment="Center" BorderBrush="Black" BorderThickness="1" Height="30"/>

                    <Label Content="Số Điện Thoại:" Grid.Row="6" VerticalAlignment="Center"/>
                    <TextBox x:Name="SdtTextBox" Grid.Row="6" Grid.Column="1" VerticalContentAlignment="Center" BorderBrush="Black" BorderThickness="1" Height="30"/>

                    <Label Content="Trạng Thái:" Grid.Row="7" VerticalAlignment="Center"/>
                    <TextBox x:Name="TrangthaiTextBox" Grid.Row="7" Grid.Column="1" VerticalContentAlignment="Center" BorderBrush="Black" BorderThickness="1" Height="30"/>

                    <StackPanel Orientation="Horizontal" Grid.Row="8" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Button Content="Hủy bỏ" Width="80" Height="30" Margin="0,0,10,0" Foreground="Blue" Click="Button_Click_3"/>
                        <Button Content="Lưu" Width="80" Height="30" Foreground="White" Background="Blue" Click="SaveStudent_Click"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>