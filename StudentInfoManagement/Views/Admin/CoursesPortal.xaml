<UserControl x:Class="StudentInfoManagement.Views.CoursesPortal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             FontFamily="Segoe UI">

    <UserControl.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.03" BlurRadius="5" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FilterLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="CleanComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="templateBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6">

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="30"/>
                                    </Grid.ColumnDefinitions>

                                    <ContentPresenter Grid.Column="0" 
                                                      IsHitTestVisible="False" 
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"
                                                      Margin="{TemplateBinding Padding}"/>

                                    <ToggleButton Grid.Column="1" 
                                                  Focusable="false"
                                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                  ClickMode="Press">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent">
                                                    <Path Data="M0,0 L5,5 L10,0" 
                                                          Stroke="#6B7280" 
                                                          StrokeThickness="1.5"
                                                          HorizontalAlignment="Center" 
                                                          VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                </Grid>
                            </Border>

                            <Popup x:Name="PART_Popup" 
                                   AllowsTransparency="True" 
                                   IsOpen="{TemplateBinding IsDropDownOpen}" 
                                   Placement="Bottom"
                                   VerticalOffset="2">
                                <Border x:Name="DropDownBorder" 
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        Background="White"
                                        BorderBrush="#D1D5DB"
                                        BorderThickness="1"
                                        CornerRadius="6">
                                    <ScrollViewer Margin="2">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="templateBorder" Property="BorderBrush" Value="#9CA3AF"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="templateBorder" Property="BorderBrush" Value="#2563EB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="5,0,5,15">
            <TextBlock Text="Quản lý Đăng Ký Tín Chỉ" FontSize="26" FontWeight="Bold" Foreground="#111827"/>
            <Button Content="+ Mở Lớp Mới" Click="BtnAddNew_Click"
                    HorizontalAlignment="Right"
                    Background="#2563EB" Foreground="White" BorderThickness="0" 
                    Padding="15,8" FontWeight="SemiBold" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bdr" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bdr" Property="Background" Value="#1D4ED8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="20" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Bộ lọc danh sách" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Tìm kiếm lớp học" Style="{StaticResource FilterLabelStyle}"/>
                        <Border Height="40" CornerRadius="6" BorderBrush="#D1D5DB" BorderThickness="1" Background="White">
                            <Grid>
                                <TextBlock Text="🔍" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="#9CA3AF"/>
                                <TextBox x:Name="txtSearch" 
                                         BorderThickness="0" Background="Transparent" 
                                         VerticalContentAlignment="Center" Margin="35,0,5,0"
                                         TextChanged="OnFilterChanged"/>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Lọc theo Học kỳ" Style="{StaticResource FilterLabelStyle}"/>
                        <ComboBox x:Name="cbSemester" 
                                  Style="{StaticResource CleanComboBoxStyle}" 
                                  SelectionChanged="OnFilterChanged" 
                                  SelectedIndex="0">
                            <ComboBoxItem Content="Tất cả học kỳ"/>
                            <ComboBoxItem Content="HK1 2024-2025"/>
                            <ComboBoxItem Content="HK2 2024-2025"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Column="4">
                        <TextBlock Text="Trạng thái" Style="{StaticResource FilterLabelStyle}"/>
                        <ComboBox x:Name="cbStatus" 
                                  Style="{StaticResource CleanComboBoxStyle}" 
                                  SelectionChanged="OnFilterChanged" 
                                  SelectedIndex="0">
                            <ComboBoxItem Content="Tất cả"/>
                            <ComboBoxItem Content="Đang mở"/>
                            <ComboBoxItem Content="Đã đầy"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="2.5*"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="1.2*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Mã LHP" FontWeight="Bold" Foreground="#6B7280"/>
                    <TextBlock Grid.Column="1" Text="Tên Môn Học" FontWeight="Bold" Foreground="#6B7280"/>
                    <TextBlock Grid.Column="2" Text="Giảng Viên" FontWeight="Bold" Foreground="#6B7280"/>
                    <TextBlock Grid.Column="3" Text="Sĩ Số" FontWeight="Bold" Foreground="#6B7280"/>
                    <TextBlock Grid.Column="4" Text="Trạng Thái" FontWeight="Bold" Foreground="#6B7280" HorizontalAlignment="Center"/>
                    <TextBlock Grid.Column="5" Text="Thao tác" FontWeight="Bold" Foreground="#6B7280" HorizontalAlignment="Center"/>
                </Grid>
                <Separator VerticalAlignment="Bottom" Background="#E5E7EB" Margin="0,0,0,5"/>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="icClassList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#F3F4F6" BorderThickness="0,0,0,1" Padding="0,15">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1*"/>
                                            <ColumnDefinition Width="2.5*"/>
                                            <ColumnDefinition Width="1.5*"/>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="1.2*"/>
                                            <ColumnDefinition Width="1*"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" Text="{Binding ClassID}" FontWeight="Bold" VerticalAlignment="Center"/>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding SubjectName}" FontWeight="Medium" Foreground="#111827"/>
                                            <TextBlock Text="{Binding Schedule}" FontSize="12" Foreground="#6B7280"/>
                                        </StackPanel>
                                        <TextBlock Grid.Column="2" Text="{Binding Lecturer}" VerticalAlignment="Center" Foreground="#4B5563"/>

                                        <Grid Grid.Column="3" VerticalAlignment="Center" Margin="0,0,20,0">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Grid Grid.Row="0" Margin="0,0,0,5">
                                                <TextBlock Text="{Binding StudentCountDisplay}" FontSize="12" FontWeight="Bold"/>
                                                <TextBlock Text="{Binding PercentageDisplay}" FontSize="12" Foreground="#6B7280" HorizontalAlignment="Right"/>
                                            </Grid>
                                            <ProgressBar Grid.Row="1" Height="8" Value="{Binding CurrentCount}" Maximum="{Binding MaxCount}" 
                                                         Foreground="{Binding ProgressBarColor}" Background="#E5E7EB" BorderThickness="0"/>
                                        </Grid>

                                        <Border Grid.Column="4" Background="{Binding StatusBgColor}" CornerRadius="12" Padding="10,4" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding StatusText}" FontSize="12" FontWeight="SemiBold" Foreground="{Binding StatusFgColor}"/>
                                        </Border>

                                        <StackPanel Grid.Column="5" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <Button Content="✏️" Click="BtnEdit_Click" Tag="{Binding ClassID}" 
                                                    Background="Transparent" BorderThickness="0" Margin="0,0,5,0" Cursor="Hand" ToolTip="Sửa"/>
                                            <Button Content="🗑️" Click="BtnDelete_Click" Tag="{Binding ClassID}"
                                                    Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="Xóa"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>